
NEW SYNTAX 0.0.5 syntax 

ALL functions are in willow exccept @define @init @endInt @ret
LIKE print, input, etc

base = { @define, @init, @endInt, @ret }

@define = {
	"willow"
}

willow = {
	printfl
	inputfl
	func
	i32, i64, i8, i8*, ls
	mut*, const
	if, else if, else
	while, for
	? <- 
	? ->
}

for funcs if you have a var in the () you have to in the func make it mut* so you can change it and print it